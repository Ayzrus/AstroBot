import { Command } from "#base";
import { ApplicationCommandType } from "discord.js";

new Command({
  name: "unlock",
  description: "[🔒] Usado para dar unlock em um canal.",
  type: ApplicationCommandType.ChatInput,
  async run(interaction) {

  }
});